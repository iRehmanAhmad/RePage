[package]
name = "repage-desktop"
version = "0.1.0"
description = "RePage — Authentic Urdu Document Publishing System"
authors = ["RePage Contributors"]
edition = "2021"

[lib]
name = "repage_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
